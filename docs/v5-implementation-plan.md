# V5 identity assurance

Preserve the exact active v4 policy and existing raw probes. Add a shared validator
that resolves native request, process, version and trace bytes by digest, derives
model/effort assurance, and rejects contradictory stronger evidence. No observation
or agent-written receipt can supply assurance authority.

Integrate that result with v5 qualification, independently assessed receipts,
native adapters, bindings and compact qualified routing records. Keep economics,
sample thresholds, risk reviews, routing modes and task scope unchanged. Test
forged claims, mismatches, aliases, missing sources, host boundaries, future
telemetry and 20-task subscription qualification reachability.

Re-run the actual upgraded frontiers, independently review captured executions,
and admit only roles supported by real evidence. Recompile and exercise every
route and fallback; verify copied skill closure, full tests, typecheck and build.
Publish the scoped result and verify GitHub CI. Preserve failures and remaining
qualification gaps honestly.
