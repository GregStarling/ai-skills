# Subscription-first v4 implementation

Preserve the current governor and delegate workflow. Separate capability qualification from economic selection without lowering task, quality, failure, latency, review, identity or freshness requirements. Keep policy v3 as an immutable historical artifact; publish v4 explicitly. Exact API/Claude Code/Codex execution provenance must prevent API success from becoming host qualification. Economics are a separately validated normalized API proxy, never subscription billing or raw-token ranking.

Implement in dependency order: policy and governance semantics; economic evidence and selection/promotion; runtime-host/receipt/refresh integration; factored versioned pack and focused consumer instructions; placeholder parser fix and medium-risk evidence assessment. Use disjoint workers for governance, pack serialization, and telemetry/probe assessment; supervisor owns runtime provenance, receipt integration, final review and release. Stabilize shared contracts before consumers depend on them.

Baseline at commit 47d6e3e: 251 tests, routing pack 182456 bytes; SKILL.md 6947 bytes and 914 whitespace-delimited words. Preserve native source evidence; do not rerun unavailable frontier models or change global clients. Add adversarial tests for unknown dollars, cheap incapable candidates, exact model/effort and host separation, measured/proxy/unknown economics, promotion limits, receipt lineage, pack integrity/closure and placeholder identities. Run full tests/types/build, built CLI and portable/publication checks, publish and confirm GitHub CI. Record actual route counts, artifact size changes, evidence gaps and manual client follow-up.

Implementation and final local verification complete: 320 tests pass across 26 files; typecheck, build, copied skill closure and exact published-pack validation pass. Built CLI resolved all 15 routes and each failed-worker fallback, enforced medium fresh context and refused the old v2 format. Six shared treatments reduce the pack to 123721 bytes; the entry skill is 613 words. The one added medium route is supported by a derived audit of original Claude raw traces. Qualified routes remain zero. See [final validation](v4-validation.md); GitHub runs the unchanged required gates on publication.
