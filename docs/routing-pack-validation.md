# Portable routing-pack validation

Validated 2026-09-10 UTC. This update connects class-specific acceptance evidence, frontier ownership of diagnosis/planning, and current-frontier refresh probes. Routes remain explicitly provisional; no full governor qualification or measured savings are claimed.

## Repository and compiler checks

All **214 tests across 24 files passed**, including 17 routing tests, 16 acceptance-evidence tests, two frontier-refresh tests and three routing CLI tests. Type checking, build, built-CLI publication validation, and the eight-file copied-folder/reference-closure check passed. All 14 real pack routes resolve and fall back correctly against a constructed inventory; actual host dispatch is checked separately. The source checkout remains uninstalled.

Coverage includes mandatory frontier review at every risk, qualified preference, provisional admission, known-failure exclusion, provider restrictions, host capability/identity/effort intersection, reviewer independence, retained incumbents, evidence expiry, publication clock checks, simulation rejection, tampering and full-project decomposition. The final audit also fixed a non-frontier economic winner hiding an eligible frontier reviewer, and provisional alternatives bypassing a governed provider restriction.

Pack generation: `2026-09-10T04:20:26.878Z`; refresh due September 17; pack expiry October 10. Each treatment also expires with its oldest required evidence/source date. Refreshing the artifact cannot renew observations.

Pack content digest: `sha256:a5f44df2af7a010ea5dcc2ec44d3a8e75de37ae443cbe9ff28845f222cdcdd17`.

The pilot compiler now joins the original installed acceptance records by class, host, model, effort and role. Five routable classes per host have matching task evidence. Research, exploration and untested fallback treatments retain explicit smoke extrapolation. Recovered UI evidence retains its assistance limits and exact rendered artifact; full-project acceptance grants no worker route. The original source dates, failures and qualification thresholds are unchanged.

Native probes of Astra and Fable 5.1 both returned explicit client-version rejections, recorded in [frontier-probes.json](../data/routing/frontier-probes.json). This explains retention of GPT5.5/high and Opus5/high on the tested CLI routes. In isolated copies, missing CLI binaries produced two persisted `unknown` records rather than aborting the report; stale probes and available models awaiting evaluation review both blocked publication without changing the existing pack.

## Updated frontier workflow acceptance

The [new workflow record](../data/routing/frontier-ownership-acceptance.json) preserves seven actual attempts with copied-file manifests, native event references, controls, checks, receipts, timing and telemetry. Four successful host/workflow combinations exercise the changed responsibilities:

| Workflow | Claude Code | Codex CLI |
| --- | --- | --- |
| Hard-debug reproduction → diagnosis → worker fix → frontier regression check | Passed on a fresh retry after the instruction correction | Passed |
| Frontier module architecture/contracts/plan → worker implementation → frontier integration check | Passed explicit complex-mode exercise | Passed explicit complex-mode exercise |

Both hosts naturally classified the original multi-file fixture as bounded work because its functional contracts were already supplied. Those two successful runs remain separate. The complex-mode trials explicitly force the class to test execution order; they do not establish automatic classification or general architectural quality. Native traces show Haiku4.5 or Spark/low doing implementation, with Opus5/high or GPT5.5/high performing frontier work and inspecting actual artifacts.

The first updated Claude hardbug run diagnosed the source but skipped executing the reproduction before dispatch, then reached the 240-second cap. Its passing artifact and model-written accepted receipt did not override that failure. The skill now explicitly requires executed failure evidence and a precise fix in the packet before dispatch. A fresh run passed that order and both independent regression suites. The harness allows 360 seconds for hard-debug and forced-plan exercises. Recovered check-command mistakes remain in the traces.

The final copied folder digest is `sha256:bda5845cfe91ccfb84313b9c5e13f0b66671dc8731316c20599958fa4bd01e76`; its manifest and digest method are in the new record. The successful Codex hardbug used the initial updated wording before the prerequisite clarification; the later trials copied the final folder exactly. No earlier trial was relabeled as a final-folder run. Two Claude hardbug receipts wrote incorrect start times; public timing uses native harness timestamps and measured durations, retaining the discrepancies. Raw model-written metadata is not authoritative evaluation evidence.

## Original installed host acceptance

Disposable projects received the complete folder under `.claude/skills/delegate/` or `.agents/skills/delegate/`. The harness invoked literal `/delegate` in Claude Code **2.1.222** and `$delegate` in Codex CLI **0.142.5**, with the project path explicit to avoid an unrelated global skill of the same name. No global skill/configuration changes or new API keys were used.

Native traces, actual artifacts, independent external graders and frontier review establish the outcomes in [the original acceptance record](../data/routing/installed-acceptance.json), retained unchanged from the 195-test release. These cases used the previous pack digest `sha256:885956442ada2677325887532cff51c2a165ca11fb2237afe69b972c011a6e86`. Model-written receipts are checked against host evidence; a model's claim alone is not dispatch or acceptance proof. UI browser checks used the supervising frontier's CUA browser and artifact hashes. That release's final tinybug installations on both hosts byte-match its eight-file consumer (folder digest `sha256:2509569ea44c7dd78170df77214f858d0cafedb5d16b8890e1937680b3053767`). Broader scenarios used earlier instruction iterations with the same original routing pack; per-case digests preserve that distinction. They are capability observations, not proof of the updated frontier workflow.

| Case | Claude Code | Codex CLI |
| --- | --- | --- |
| Tiny quantity-default bug | Accepted | Accepted; separate fault-injection repair trial |
| Mechanical API rename | Accepted; policy-induced Haiku → Sonnet fallback | Accepted |
| Bounded pagination feature | Accepted | Accepted |
| Static semantic/responsive UI | Accepted with harness recovery and browser review | Accepted with harness recovery and browser review |
| Async stale-result/rejection bug | Accepted; one coherent worker | Accepted; one coherent worker |
| Multi-component task feature | Accepted | Accepted |
| Three-module taskboard project | Accepted; three workers | Accepted; two workers and real contract-error repair |

The UI fixture intentionally requires no JavaScript Add-task behavior. Both rendered artifacts fit 320px and 1280px, expose labels and native checkboxes, and retain visible keyboard focus. The project fixture exercises store/view ownership, dependency-ordered integration, escaping and behavioral checks; it is not evidence for a production SaaS build.

Codex dispatched Spark at low effort, with model/effort confirmed in native spawn arguments and child configuration. Its frontier coordinator was GPT5.5/high. Claude normally dispatched Haiku4.5 without an unsupported effort override and used Opus5/high for frontier verification. The constrained fallback selected Sonnet5/low through a native authenticated child CLI. Configured effort is not proof of served effort; runtime Claude message identities and Codex configuration observations remain distinct.

## Failures found and resolved

- Empty Claude settings sources disabled project slash-skill discovery. The harness now preserves project settings.
- Early coordinators guessed the pack layout, duplicated reviews or inherited recursive delegation instructions. The skill now states the actual layout, bounded-packet behavior and eligible coordinator review rule.
- A Claude child edited correctly but could not run its Bash checks under `acceptEdits` alone. Frontier checks supplied the missing evidence; the host guide now requires task-scoped permissions for already-authorized child verification.
- Both initial UI runs reached correct artifacts but hit the 240-second harness cap before receipt writing. Their failures remain recorded. Browser review was captured against exact artifact hashes, and frontier completion saved accepted receipts. A Codex resume initially defaulted to read-only; explicitly restoring workspace-write completed the receipt. Future UI/project harness limits are 360/480 seconds. These are recovered, assisted completions, not unattended first-pass successes.
- A UI receipt confused pack publication time with task start. Acceptance timing uses the harness clock; the original discrepancy is preserved. Receipt guidance now explicitly requires actual run timestamps and pack candidate/tier identity for the verifier.

## Evidence limits

The pack has **14 scoped provisional routes and zero qualified routes**. Six treatments have actual native smoke evidence, official metadata and frontier review. The scope is low-risk local JavaScript/HTML work and supplied-local-material analysis; research and exploration scopes have not received their own broader empirical qualification.

The original qualification thresholds remain unchanged. Claude list prices/client estimates are not subscription bills. Codex per-run dollar cost is unknown; resumed token totals may be cumulative. No completion-cost or token-savings advantage is established by this suite. Portable installation and local task execution are proven within the stated fixtures and recovery limits; broad language coverage, high-risk routes and general project reliability require further evidence.
