# Remaining delegate completion: authorization and execution plan

The user approved the remaining M4/M5 scope and a hard ceiling of **130 additional model executions** in the Codex maintainer task on 2026-09-11. This includes implementation workers/reviewers, failed launches, retries, capacity checks, matched trials, calibration and renewal runs. Zero earlier offline M3 preparations consume this budget. Publication to the remote and global installation remain outside this approval.

Decisions: D1 full renewal; D2 retain the implemented learning defaults; D3 the existing two ai-foreman harvested fixtures; D4 run the core M4 campaign; D5 before-folder comparisons on both hosts where capacity permits; D6 require at least two quality-gated pairs per shape and at least half won or tied before funding expansion; D7 fresh Fable/Astra calibration, with admission only on passing evidence; D8 v4 only when an expansion is funded. Research expansion also requires the research rerun to complete with matched receipts. Medium expansion requires the specified fresh independent review and identity assurance. No gate failure is bypassed to spend the budget.

Implementation sequence:

1. Finish persistent cross-phase accounting, sanitized live capacity checks, frozen-folder/expiry checks, and independent trace-review evidence.
2. Execute M4 in the planned host order, review each pair, and preserve all blocked or failed runs. Run no replacement trials. Publish the evidence index locally.
3. Apply the recorded gates. Build and test renewal assembly, expiry demotion and publication guards; archive September inputs byte-identical before replacing canonical files.
4. Run authorized renewal/calibration and only eligible expansion work, compile and validate the complete consumer folder, and update the status index. Commit verified increments locally.

The live machine-readable budget is `artifacts/delegate-completion-2026-09-11/budget.json`. Raw artifacts remain local; committed evidence references their digests. Provider-limit stops apply to the affected execution host; unresolved or excess accounting stops further launches. No unused allowance is a spending target. Historical reports retain their original verdicts and are only extended with dated addenda where the plan permits.
