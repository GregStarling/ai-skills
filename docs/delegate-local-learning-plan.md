# Local learning implementation

Implement the approved quality-first local feedback loop, bounded live-documentation research, and advisory session reminders. Shared skill/pack remain versioned; user state stays outside the installation. Global installation is separate; repository publication follows an explicit ship request.

Checkpoints: (1) portable built-in Node helper and strict v2 receipts; (2) conservative local advice and resolver/importer integration; (3) skill lifecycle, research and reminder guidance; (4) harness corrections and offline adversarial checks; (5) four isolated live trials, at most eight visible model invocations and 240 seconds per coordinator. Preserve failures and incomplete validation honestly. Historical pilots do not validate new behavior.

Implemented the local task lifecycle, canonical v2 exports, v1 import compatibility, conservative optional resolver preferences, focused research guidance, and safe once-per-session reminders with explicit local frequency controls. The helper adds `capture` to execute an already-required check once and preserve actual output provenance; no extra model call is involved. See [validation](delegate-local-learning-validation.md) for executed checks, exact tested package identities, live account/deadline limits, and remaining acceptance work. No installed user skill or routing-pack contents were changed.
