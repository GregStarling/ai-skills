# Model Governor

Read docs/model-governor-spec.md and docs/spec.json before implementation. Build milestone one only: constitution, schemas, deterministic validator and synthetic tests. Foreman owns task planning and isolated worktrees. Preserve its acceptance evidence and do not bypass gates.

Keep model IDs out of constitution policy. Never invent quality/confidence scores. Initial policy thresholds are declared proposals, not empirical facts. Validate exact policy and evidence identity, recompute decisions independently, and reject synthetic evidence in production mode. No live provider calls, runtime adapters, global installations, remote pushes, or new GitHub repositories in this milestone.

Write docs/plan.md before substantial application code. Prefer strict TypeScript schemas, pure functions, stable rule IDs and table-driven adversarial tests. Run npm test, npm run typecheck and npm run build once those scripts exist. Never mark unrun checks as passed.
